# EXAMPLE CONTRACT ONLY: replace ALL atom types/IDs with your mapping.
# Same element may belong to polymer, solvent, anion or surface: use chemical membership,
# not a blanket "group polymer type C H F" selection.
# Each complete polymer chain and each solvent/anion must have unique positive mol-ID.
# Illustrative ranges only, NOT ready-to-run definitions:
# group polymer molecule 1:20
# group bbtypes type 1 3
# group backbone intersect polymer bbtypes
# group solvent molecule 21:120
# group anion molecule 121:140
# group Li type 15
# group substrate type 20 21
# group particles type 22 23
# group anchor id 9001:9200
# group grip id 301:350
# group probe molecule 1
# group donor_poly type 4
# group donor_solvent type 10
# group donor_anion type 18
# variable Li_type index 15
# variable poly_donor_type index 4
# variable solv_donor_type index 10
# variable anion_donor_type index 18
# If a group is absent and not required by the chosen task, define it as empty.
# group particles empty
# group solvent empty
# grip: define fixed atom IDs from equilibrated configuration, not a dynamic moving slice.
# anchor subset of substrate; grip subset of polymer; probe complete chain subset polymer.
# An atom cannot be time-integrated twice. Hooks must contain constraints only.
