# 验证记录

日期：2026-09-25。

## 实际完成

- 12项Python数值测试全部通过：线性模量拟合、分离功单位、FFT相关函数与直接计算一致、相关积分、多个时间原点MSD、扩散/电导单位、已知谐势WHAM、无重叠窗口拒绝、同组成不同排列的序列描述符、连续/间歇配位相关、轨迹排序/盒读取、共同漂移处理不删除不同离子间相对运动。
- Python源文件语法解析通过；文件内字面量include/jump目标存在。
- QM导出命令用一个合成完整分子测试，成功生成相同几何的初态/氧化态ORCA输入；未执行QM引擎。
- 批量生成器实际生成12条结构/工况/速度种子命令及清单，没有执行生产任务。
- 生成26个固定电荷LAMMPS玩具模型冒烟测试命令，覆盖五目标常规分支及6类应变的正负方向；尚未在LAMMPS执行。

## 未完成、不能声称已验证

- 本环境没有LAMMPS可执行程序，尝试获取运行依赖时网络解析不可用。因此仅做了输入文件静态核对，没有LAMMPS运行时语法/版本兼容性验证。
- 没有用户真实PVDF/共聚结构的bulk、界面、溶液或含盐模型；没有经过选定/验证的力场和交叉参数。
- 没有任何生产性质数值、统计收敛、实验一致性、晶型识别或缠结结果。
- 没有执行ORCA/DFT、ReaxFF或NEB；QM输入和反应脚本均须针对实际化学环境配置并验证。

## 在目标计算环境的下一步

1. 运行 `python -m unittest discover -s tests -v`。
2. 运行 `python tests/smoke_lammps.py --lmp /path/to/lmp --work-dir /tmp/pvdf_smoke`。该玩具势仅测试程序路径，不代表PVDF。检查每项console.txt，修正版本/缺包问题。
3. 为一个真实候选完成建模和赋参，先run 0/短运行并核对能量、力、拓扑、原子分组、电荷、温度与边界。
4. 再做时间步长、平衡、尺寸、速率和采样收敛，随后开展多序列比较。
